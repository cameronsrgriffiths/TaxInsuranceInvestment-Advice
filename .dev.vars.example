# Copy this file to `.dev.vars` for local development (`npm run dev`).
# `.dev.vars` is gitignored. In production, set the secret instead with:
#   wrangler secret put ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=sk-ant-...
